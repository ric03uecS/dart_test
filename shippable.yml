language: python

python:
   - 3.2

before_script:
 - mkdir -p ./shippable/testresults

install: true

script: cp testresults.xml  ./shippable/testresults

archive: true
build_image:
  name: shippable/minv2
  net: host
  privileged: true
